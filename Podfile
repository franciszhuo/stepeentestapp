
platform :ios, '9.0'

target 'step test app' do
  use_frameworks!

  # Pods for step test app
  
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Firebase/Core'
  pod 'Firebase/Database'

end
